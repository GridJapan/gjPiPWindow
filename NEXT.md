# 状態メモ

## 済んだこと（2026-07-17）

- 再起動テスト合格。仮想3台・解像度・壁紙・配置すべて復元（検証対象だった解像度も残った）
- 宣伝用の実録画3本を撮影 → `docs/assets/`（合わせ鏡・操作モード・集める）。
  元素材とGIF版は `~/Downloads/pip_sozai/`、撮影用デモページは `~/claude/live/nazo_demo_stage/`
- **ビデオフィルタ17種を実装**（`VideoFilter.swift`）。`CALayer.filters` に CIFilter チェーンを
  載せる方式でゼロコピー維持。PiP ごとのサブメニューから複数同時適用・すべて解除
- GitHub Pages の宣伝ページを `docs/` に作成（8-Bit Orbit テイスト）。
  文言は grok 壁打ちを2周通した事実ベース版。比較表の BetterDisplay 列は
  公式 README / betterdisplay.pro を実測して裏取り（PiP・フィルタは Pro「*」・$21.99、
  PiP越しのマウス操作は記載なし）
- MIT LICENSE 追加

## 撮影の教訓（次に録るとき）

- 舞台は「デスクトップ3」（空のSpace）。録画中にユーザーが操作すると Space が
  切り替わって作業画面が写る。**録画中は手を触れない**時間を確保してもらう
- デモページは全画面にしない（Chrome の「esc長押し」トーストがフォーカスの度に出る）。
  映す先は一番小さい仮想ディスプレイ（1280×720）＝カーソルがほぼ原寸で写る
- メニュー操作は CGEvent では届かない。AX（System Events）で叩く。PiP 上の操作は CGEvent
- 「集める」はカスケードが 36px 刻みなので、3枚戻っても1枚に見える。絵として弱いのが未解決

## 未解決の宿題

- **PiP を左端にくっつけると勝手に中央へ戻る**（原因未特定。タイル配置の仮説あり、
  検証には数十秒 PiP に触らない時間が要る）
- Issue #1: フォーカスされていない PiP の音声ミュート（実現可能か未検証）
- 未実装: クロップ・回転・ズーム・グローバルホットキー・クリック透過
  （ページにも「未実装」と明記してある。実装したらページの比較表も更新すること）
